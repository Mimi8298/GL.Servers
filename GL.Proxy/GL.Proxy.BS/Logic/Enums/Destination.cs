namespace GL.Proxy.BS.Logic.Enums
{
    /// <summary>
    /// Specifies if a packet is client/server sided
    /// </summary>
    public enum Destination
    {
        FROM_CLIENT,
        FROM_SERVER
    }
}